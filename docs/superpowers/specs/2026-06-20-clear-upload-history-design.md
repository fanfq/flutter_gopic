# 清除上传历史

## 目标

在 Cloud 配置页的“图床缓存”卡片中，紧邻“清除本地缓存”新增“清除上传历史”按钮。

## 行为

- 点击后先显示确认弹窗。
- 确认后清空所有云服务配置的本地上传历史索引。
- 保留本地缓存文件及云端文件。
- 历史模型通知监听者，图床页随即不再显示任何历史上传记录。

## 实现与验证

- 复用 `HistoryService.clear(deleteCache: false)`，不新增重复的存储清理逻辑。
- 扩展服务测试，断言清理历史后缓存文件仍存在。
- 运行完整 Flutter 测试集和静态分析。
